<!DOCTYPE html>
<html lang="en">
    
    <!-- THE HEAD TAG PARTIAL -->
    <%- include("../partials/head") %>

    <body>
        <!-- THE HEADER TAG PARTIAL -->
        <%- include("../partials/header") %>
        <main>
            <a href="/auth/logout"><button class="button">Logout</button></a>
            <nav><a href="/activities/new">create a new activity</a></nav>
            <h1>index page</h1>
            

            <ul>
                <% for(let i = 0; i < activity.length; i++){ %>
                    <li>
                        The 
                        <a href="/activities/<%= activity[i].id %>">
                        <%= activity[i].name %> 
                    </a>
                        is <%= activity[i].description %>.
                    
                        <!-- now and if else statement for the next sentence -->
    
                        <% if(activity[i].booked === true){ %>
                            This activity has been booked.
                        <% } else { %> 
                            This activity has not been booked.
                        <% } %>
                        <!-- edit button -- when clicked goes to form... form completed? goes to update(put route) -->
                        <a href="/activities/<%=activity[i].id; %>/edit">Edit</a>
                        <!-- delete button before list tag closes -->
                        <form action="/activities/<%=activity[i].id; %>?_method=DELETE" method="POST">
                            <input type="submit" value="DELETE">
                        </form>
                    </li>
                <% } %>







        </main>
        <!-- THE HEAD TAG PARTIAL -->
        <%- include("../partials/footer") %>
    </body>
</html>


